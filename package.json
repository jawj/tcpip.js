{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @tcpip/wire -w @tcpip/dns -w @tcpip/dhcp -w @tcpip/v86 -w tcpip",
    "test": "npm run test --workspaces --if-present"
  }
}
